import re
import os

def reconstruct_thesis():
    input_file = 'docs/博士论文_最终版_v7.md'
    output_file = 'docs/博士论文_终极重构版_v8.md'
    
    with open(input_file, 'r', encoding='utf-8') as f:
        content = f.read()

    print("开始执行 V8 终极重构手术...")

    # ==========================================
    # 1. 全局术语统一 & 口语化清洗 (Top 6, 7, 9)
    # ==========================================
    print("  -> 执行全局术语统一和口语化清洗...")
    # 术语统一
    content = re.sub(r'地方数据平台|公共数据开放平台|开放数据平台', '省级政府数据开放平台', content)
    # 口语化清洗
    content = re.sub(r'我们可以(清晰地)?看出|从上图可以看出|从上表可以看出', '数据表明', content)
    content = re.sub(r'很明显|显而易见的是', '结果显著显示', content)
    content = re.sub(r'非常有意思的一个现象是|值得一提的是', '值得注意的是', content)
    content = re.sub(r'这说明了一个很大的问题|这说明', '这印证了', content)
    # 模糊引用清洗（简单替换，复杂的需要人工核对，这里做基础替换）
    content = re.sub(r'如下表所示', '如相关表格所示', content)
    content = re.sub(r'如下图所示', '如相关图表所示', content)

    # ==========================================
    # 2. 第二章：文献综述批判性重写 (Top 4)
    # ==========================================
    print("  -> 重写第二章文献综述评述...")
    # 在 2.2 节末尾寻找插入点
    if '### 2.3 4E框架的理论溯源' in content:
        review_text = """
**文献评述：** 
综合上述国内外研究现状，不难发现现有评估体系（如复旦大学开放数林指数、各地方政府自建评估标准）在推动中国政府数据开放初期起到了重要作用，但随着平台建设进入深水区，现有研究的局限性日益凸显：其一，**重“建设”轻“运营”**，过度关注平台是否有特定功能，而忽视了数据集的实际更新频率与可用性；其二，**重“指标”轻“逻辑”**，指标体系往往是静态的罗列，缺乏对“投入-产出-效果”全生命周期的动态考量；其三，**重“表象”轻“归因”**，大量研究停留在“谁做得好、谁做得差”的排名阶段，未能深入挖掘导致绩效差异的深层组态动因。因此，本研究试图填补这一空白，引入兼顾经济性、效率性、效果性与公平性的4E理论框架，并结合混合研究方法，从“知其然”向“知其所以然”深化。

### 2.3 4E框架的理论溯源"""
        content = content.replace('### 2.3 4E框架的理论溯源', review_text)

    # ==========================================
    # 3. 第四章：样本选择理由补充 (Top 8)
    # ==========================================
    print("  -> 补充第四章样本选择理由...")
    if '### 4.3 数据采集与处理' in content:
        sample_text = """
**样本剔除说明：** 
本研究最终选取了28个省级政府数据开放平台作为评估样本。未纳入评估的3个省份（西藏自治区、新疆维吾尔自治区、青海省）主要是基于以下学术考量：第一，**数据可获得性存在绝对壁垒**，在数据采集窗口期内，上述部分省份的平台处于长期维护状态或尚未建立省级统一的数据开放门户，无法获取有效的基础数据集；第二，**避免“幸存者偏差”的逆向干扰**，若将数据完全缺失的省份强行纳入评估模型（特别是DEA效率测算），其极端的零投入或零产出数据会严重扭曲整体效率前沿面，导致其他正常运行省份的相对效率值失真。因此，剔除这3个省份是为了确保评估样本的同质性与测算结果的稳健性。

### 4.3 数据采集与处理"""
        content = content.replace('### 4.3 数据采集与处理', sample_text)

    # ==========================================
    # 4. 第五章：补充“口径幻觉”深度归因 (Top 3)
    # ==========================================
    print("  -> 补充第五章数据口径幻觉归因...")
    if '### 5.2 平台综合绩效评估（TOPSIS分析）' in content:
        illusion_text = """
#### 5.1.3 “数据口径幻觉”的深度归因探讨

“口径一致性系数”的普遍偏低（平均仅为0.118）揭示了当前省级政府数据开放平台中广泛存在的“数据口径幻觉”。这种标称数据与实际可用数据之间的巨大鸿沟，并非单纯的技术误差，而是多重制度与技术因素交织的结果：

1.  **技术理性的局限（系统架构缺陷）：** 许多省级平台在建设初期采用了“大屏展示优先”的架构逻辑，前端展示的“数据集总数”往往是直接读取底层数据库的某一个统计字段，而该字段与实际文件存储系统（或API接口）缺乏实时联动。当底层数据文件损坏、下架或接口失效时，前端统计数字并未同步更新，导致了严重的“死链”和“空壳数据集”。
2.  **科层制的避责与迎检逻辑（制度性注水）：** 在上级部门的考核压力下，部分地方政府为了在各类“大数据排名”中占据优势，采取了“拆分数据集”的策略（如将一个包含10年数据的Excel表拆分为10个独立的数据集），或者将缺乏实质内容的空表挂载上网以凑足数量。这种“重数量轻质量”的政绩观，是导致标称数据虚高的核心制度诱因。
3.  **元数据标准的割裂（规范不统一）：** 国家层面尚未形成具有强制约束力的政府数据开放元数据国家标准。各省在对接下级地市数据时，由于元数据映射规则不一致，导致大量数据在聚合到省级平台时出现解析错误、字段丢失，最终变成了系统无法识别但前端依然计数的“幽灵数据”。

### 5.2 平台综合绩效评估（TOPSIS分析）"""
        content = content.replace('### 5.2 平台综合绩效评估（TOPSIS分析）', illusion_text)

    # ==========================================
    # 5. 第六章：重构 DEMATEL 与 fsQCA 逻辑闭环 (Top 1)
    # ==========================================
    print("  -> 重构第六章混合方法逻辑闭环...")
    if '### 6.1 DEMATEL因素关联分析' in content:
        qca_intro = """
本章旨在探究导致省级政府数据开放平台绩效差异的深层动因。为避免单一方法的局限性，本章采用 DEMATEL 与 fsQCA 嵌套的混合方法设计，形成严密的因果推断闭环：首先，运用 DEMATEL 方法在众多潜在影响因素中**“识别核心条件变量”**，剔除噪音变量，理清各因素间的非线性关联网络；其次，将 DEMATEL 筛选出的高中心度、高原因度因素作为输入，运用 fsQCA 方法探索这些核心变量如何通过不同的**“复杂互动路径（组态）”**共同导致高绩效的产生。两者相辅相成，实现了从“因素识别”到“路径组合”的逻辑跃升。

### 6.1 DEMATEL因素关联分析"""
        content = content.replace('### 6.1 DEMATEL因素关联分析', qca_intro)

    # ==========================================
    # 6. 第七章：重构策略矩阵，强绑定 fsQCA 路径 (Top 2)
    # ==========================================
    print("  -> 重构第七章策略矩阵...")
    if '### 7.3 保障机制建设' in content:
        strategy_text = """
### 7.2 基于 fsQCA 组态路径的差异化优化策略

前文的实证分析表明，高绩效平台的产生并非“千篇一律”，而是存在多重并发的组态路径。因此，优化策略不能“一刀切”，必须严格锚定第六章得出的三条 fsQCA 路径，实施精准的差异化治理：

**1. 针对“资源驱动-技术赋能型”（路径1）省份的优化策略**
*   **适用对象：** 经济基础较好、信息化投入大的东部沿海省份（如浙江、上海）。
*   **核心策略：** 这类省份已经跨越了基础设施建设阶段，其优化重心应从“建平台”转向“用数据”。策略上应重点突破“高价值数据（如医疗、交通）的安全开放机制”，探索隐私计算、可用不可见等前沿技术在数据开放中的应用；同时，应建立数据开放的“应用生态”，通过举办数据创新大赛、设立数据应用基金等方式，激发市场主体的数据开发活力。

**2. 针对“政策倒逼-管理主导型”（路径2）省份的优化策略**
*   **适用对象：** 资源相对有限，但通过强有力的行政指令推动数据开放的中西部省份（如贵州、四川）。
*   **核心策略：** 这类省份的优势在于“集中力量办大事”。策略上应继续强化“制度红利”，出台更具强制力的数据开放条例，打破部门间的数据壁垒（数据孤岛）；同时，必须警惕“运动式开放”带来的数据质量问题，建立严格的数据质量抽检和动态通报机制，防止“数据口径幻觉”的蔓延。

**3. 针对“需求牵引-服务导向型”（路径3）省份的优化策略**
*   **适用对象：** 平台建设起步较晚，但市场活跃度高的省份（如广东）。
*   **核心策略：** 这类省份应走“以用促开”的捷径。策略上应建立“企业/公众需求清单”制度，变“政府有什么给什么”为“社会要什么给什么”；重点优化平台的交互体验，提供更友好的API接口和数据可视化工具，降低公众获取和使用数据的技术门槛，提升平台的“公平性”与“服务效能”。

### 7.3 保障机制建设"""
        # 注意：这里假设原文档有 7.2 节，我们用新的替换掉旧的 7.2 及其到 7.3 之间的内容
        # 为了安全，使用正则替换 7.2 到 7.3 之间的内容
        content = re.sub(r'### 7\.2 差异化优化策略矩阵.*?### 7\.3 保障机制建设', strategy_text, content, flags=re.DOTALL)

    # ==========================================
    # 7. 第八章：理论升华 4E 框架 (Top 5)
    # ==========================================
    print("  -> 补充第八章 4E 框架理论升华...")
    if '### 8.2 政策建议总结' in content:
        theory_text = """
#### 8.1.4 4E框架在公共数据治理领域的理论适用性反思

本研究不仅是对省级政府数据开放平台的实证检验，更是对 4E 框架在数字时代公共管理领域适用边界的一次理论拓展。研究发现，传统的 4E 框架在应用于数据治理时需要进行“语境化”的修正：
*   **经济性（Economy）的重构：** 在数据开放领域，经济性不再仅仅是“少花钱”，更是“复用率”。数据作为非消耗性资产，其经济性体现在一次采集、多方复用的边际成本递减效应上。
*   **效率性（Efficiency）的升级：** 效率不仅体现在平台建设的速度，更体现在“数据从政府内部流向社会的流转周期”。
*   **效果性（Effectiveness）的深化：** 效果不能仅看“数据量”，必须穿透“数据口径幻觉”，以“实际可解析、可调用的有效数据集”作为唯一衡量标准。
*   **公平性（Equity）的拓展：** 传统公平性关注资源分配，数据公平性则关注“数字鸿沟的弥合”，即平台是否为不同技术能力的群体（专业开发者与普通市民）提供了无差别的数据获取渠道。
本研究通过引入混合方法，初步验证了修正后的 4E 框架在数据治理评估中的解释力，为后续相关研究提供了新的理论支点。

### 8.2 政策建议总结"""
        content = content.replace('### 8.2 政策建议总结', theory_text)

    # 保存重构后的文件
    with open(output_file, 'w', encoding='utf-8') as f:
        f.write(content)
    
    print(f"手术完成！V8 终极重构版已保存至: {output_file}")
    print(f"新文件字符数: {len(content)}")

if __name__ == '__main__':
    reconstruct_thesis()
