# 2026-04-24 工作记录

## 本地开发环境搭建
- 使用 Python 3.11.9 创建虚拟环境（Python 3.15 不兼容 lxml）
- 安装所有依赖：Flask, requests, beautifulsoup4, pandas, numpy, lxml, gunicorn
- 初始化本地 SQLite 数据库：data/ogd_database.db
- 创建启动脚本：start_local.bat 和 start_local.ps1
- 本地访问地址：http://127.0.0.1:5000，密码：123

## GitHub推送问题解决
- 历史 commit 中的腾讯云 Secret ID 导致 Push Protection 拦截
- 用户已在本地清理（git filter-repo 或类似操作）
- 重新添加 remote origin 后推送成功
- 服务器端因 git clean/reset 丢失了 venv，已重新创建并安装依赖
- 服务已恢复正常（HTTP 302）

## 服务器问题修复
- 服务器端 git pull 遇到合并冲突（数据库文件和日志文件）
- 使用 git reset --hard origin/main 强制同步
- 因重置导致 venv 丢失，重新创建虚拟环境并安装依赖
- 数据库已备份到 /tmp/ogd_database_backup_*.db
- 服务重启成功

## 本地开发工作流确立
1. 本地修改代码 → 2. 本地测试调试 → 3. 提交到 GitHub → 4. 部署到服务器
- 本地 debug=True，自动重载
- 服务器通过 systemd 管理服务
- 无需 SSH 即可本地开发
